{
    "name": "mapasculturais",
    "private": true,
    "scripts": {
        "dev": "lerna bootstrap && lerna run --parallel module:dev",
        "build": "lerna bootstrap && lerna run --parallel module:build"
    },
    "devDependencies": {
        "autoprefixer": "^10.4.2",
        "chokidar-cli": "^3.0.0",
        "cssnano": "^5.0.16",
        "esbuild": "^0.14.14",
        "lerna": "^4.0.0",
        "postcss": "^8.4.5",
        "postcss-cli": "^9.1.0",
        "sass": "^1.49.0"
    }
}
