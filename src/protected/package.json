{
    "name": "mapasculturais",
    "private": true,
    "scripts": {
        "dev": "lerna bootstrap && lerna run --parallel module:dev",
        "build": "lerna bootstrap && lerna run --parallel module:build",
        "watch": "lerna bootstrap && lerna run --parallel module:watch"
    },
    "devDependencies": {
        "esbuild": "^0.14.14",
        "fast-glob": "^3.2.11",
        "laravel-mix": "^6.0.41",
        "laravel-mix-esbuild": "^1.1.0",
        "lerna": "^4.0.0",
        "resolve-url-loader": "^5.0.0",
        "sass": "^1.49.0",
        "sass-loader": "^12.4.0"
    }
}
