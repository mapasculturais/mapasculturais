tags:
  - Eventos
summary: Recupera um único evento
description: Permite a recuperação de detalhes de um evento específico usando seu ID.
parameters:
  - $ref: '../../parameters/path/id.yaml'
responses:
  '200':
    description: Detalhes do evento recuperado.
    content:
      application/json:
        schema:
          $ref: '../../schemas/event.yaml'
