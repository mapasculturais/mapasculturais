tags:
  - Agentes
summary: Recupera um único agente
description: Recupera um único agente
parameters:
  - $ref: '../../parameters/path/id.yaml'
responses:
  '200':
    description: Success
    content:
      application/json:
        schema:
          $ref: '../../schemas/agent.yaml'
