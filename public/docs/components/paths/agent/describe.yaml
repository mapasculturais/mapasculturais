get:
  tags:
    - Agentes
  summary: Recupera os detalhes de mapeamento da entity Agente
  description: Recupera os detalhes de mapeamento da entity Agente
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../schemas/describe-item.yaml'