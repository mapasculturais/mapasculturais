post:
  tags:
    - Agentes
  summary: Atualiza o profile do usuário logado para o profile atual
  description: Atualiza o profile do usuário logado para o profile atual
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            type: boolean
            example: true
            description: "Profile atualizado com sucesso"
    '404':
      $ref: '../../responses/error/404.yaml'