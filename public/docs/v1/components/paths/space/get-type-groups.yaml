get:
  tags:
    - Espaços
  summary: Recupera uma lista de tipos de espaço de forma agrupada
  description: Recupera uma lista de tipos de espaço de forma agrupada
  responses:
    '200':
      description: Sucesso
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../schemas/space-type-group.yaml'
