get:
  tags:
    - Projetos
  summary: Recupera uma lista de tipos de projeto de forma agrupada
  description: Recupera uma lista de tipos de projeto de forma agrupada
  responses:
    '200':
      description: Sucesso
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../schemas/project-type-groups.yaml'
