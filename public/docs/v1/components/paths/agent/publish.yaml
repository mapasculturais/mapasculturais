post:
  tags:
    - Agentes
  summary: Publica um agente
  description: Publica um agente que foi criado
  parameters:
    - in: path
      name: id
      schema:
        type: integer
      required: true
      description: Identificador do agente
  responses:
    '200':
      $ref: '../../responses/agent/publish.yaml'
    '404':
      $ref: '../../responses/error/404.yaml'
