post:
  tags:
    - Agentes
  summary: Arquiva um agente
  description: Arquiva um agente que foi criado
  parameters:
    - in: path
      name: id
      schema:
          type: integer
      required: true
      description: Identificador do agente
  responses:
    '200':
      $ref: '../../responses/agent/archive.yaml'
    '404':
      $ref: '../../responses/error/404.yaml'
