get:
  tags:
    - Eventos
  summary: Recupera a lista de tipos de eventos
  description: Recupera a lista de tipos de eventos
  responses:
    '200':
      description: Um array com os vários tipos que um evento pode ter
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../schemas/event-type.yaml'