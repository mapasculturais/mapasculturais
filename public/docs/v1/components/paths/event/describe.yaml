get:
  tags:
    - Eventos
  summary: Recupera os detalhes de mapeamento da entity Eventos
  description: Recupera os detalhes de mapeamento, incluindo os campos disponíveis e os tipos de dados, da entity Eventos.
  responses:
    '200':
      description: Sucess
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../schemas/event-describe.yaml'
